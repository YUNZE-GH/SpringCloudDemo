spring:
  application:
    name: config-client
  cloud:
    config:
      name: config-client
      profile: dev
      uri: http://localhost:9001/
      label: main
server:
  port: 9002
eureka:
  client:
    service-url:
      defaultZone: http://localhost:9000/eureka/